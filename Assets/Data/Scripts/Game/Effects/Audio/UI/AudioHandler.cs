using UnityEngine;
using UnityEngine.UI;

/*
public class AudioHandler : MonoBehaviour
{
    [SerializeField] Slider _sliderAmbient;

    [SerializeField] Slider _sliderSfx;
    [SerializeField] Slider _sliderMusic; 

    public float SfxVolume;

    public float AmbientVolume;

    public float MusicVolume;

    GameManager manager;

    // Start is called before the first frame update
    void Start()
    {
        DataCheck();
        manager = FindObjectOfType<GameManager>();
        //gameManager = GetComponentInParent<GameManager>();
    }


    private void DataCheck()
    {
        /*
        if (PlayerPrefs.HasKey("AudioVolume"))
        {
            PlayerPrefs.SetFloat("AudioVolume", 0.5f);
        }
        else
            Load();
        
    }



    public void ChangeVolumeAmbient()
    {
        AmbientVolume = _sliderAmbient.value; 

        manager.HandleAmbientVolume(AmbientVolume);
    }

    public void ChangeVolumeSFX()
    {
        //SfxVolume = _sliderAmbient.value;

        GameManager.OnSFXValueChange.Invoke(SfxVolume);
        print(SfxVolume);
        //Save();
        
    }

    
    private void Load()
    {
        //_slider.value = PlayerPrefs.GetFloat("AudioVolume");
    }

    private void Save()
    {
        //gameManager.HandleAmbientVolume(AmbientVolume);
        //PlayerPrefs.SetFloat("AudioVolume", _slider.value);
    }

    
}
    */

